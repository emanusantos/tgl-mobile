import React from 'react';
import { View } from 'react-native';

export default function FocusBar(): JSX.Element {
    return (
        <View style={{ position: 'absolute', width: 30, height: 4, backgroundColor: '#B5C401', borderRadius: 6, bottom: 60 }}></View>
    );
};
